# EQM-082 Sub-tasks

Single P0 orchestrator-direct task — no split needed.

1. Barrier logic in EQPresentationBuffer.enqueue() (_barrier_flush helper)
2. Tests: selective flush (only conflict events); no-dependency no-flush; neutrality holds
3. Self-review + queue proof log + commit
