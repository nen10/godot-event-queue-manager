# EQM-081 Sub-tasks

Single P0 orchestrator-direct task — no split needed.

1. EQPresentationPolicy Resource (flush rule authoring)
2. EQPresentationBuffer runtime (policy-driven enqueue/flush/coalesce)
3. Presentation-neutrality property test (policy variation → identical simulation trace)
4. EQError code (PRESENTATION_POLICY_CLASS_CONFLICT), API-surface wiring, golden re-baseline
